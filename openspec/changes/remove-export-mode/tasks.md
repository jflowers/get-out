## 1. Remove ExportMode from Models and Config

- [x] 1.1 Remove `ExportMode` type, `ExportModeAPI`, and `ExportModeBrowser` constants from `pkg/models/models.go`
- [x] 1.2 Remove `Mode` field from `ConversationConfig` struct in `pkg/config/types.go`
- [x] 1.3 Remove `isValidExportMode()` function and its call in `validateConversationConfig()` from `pkg/config/config.go`
- [x] 1.4 Remove `FilterByMode()` method from `ConversationsConfig` in `pkg/config/config.go`

## 2. Remove Mode from CLI Commands

- [x] 2.1 Remove `listMode` variable, `--mode` flag registration, and mode filtering logic from `internal/cli/list.go`; remove mode display from `listCore()` output
- [x] 2.2 Remove `Mode: %s` line from `formatExportDryRun()` in `internal/cli/export.go`
- [x] 2.3 Remove `Mode: "browser"` from conversation entries generated by `discover` command if present in `internal/cli/discover.go`

## 3. Update Tests

- [x] 3.1 Remove `Mode:` field from all `ConversationConfig` literals in `internal/cli/list_test.go`; remove `TestListCore_FilterByMode` test
- [x] 3.2 Remove `Mode:` field from all `ConversationConfig` literals in `internal/cli/export_core_test.go`
- [x] 3.3 Remove `TestFilterByMode` test and mode assertions from `pkg/config/config_test.go`; update mode validation test cases
- [x] 3.4 Remove `Mode:` field from conversation test data in any other test files that reference it

## 4. Verification

- [x] 4.1 Run full test suite: `go test -race -count=1 ./...` — all tests MUST pass
- [x] 4.2 Verify existing conversations.json with mode field still loads: create a temp file with `"mode": "api"` and verify `LoadConversations` succeeds
- [x] 4.3 Verify `go build ./...` succeeds with no references to removed symbols
